.sidebar
  .sidebar__name
    .user 
      .user__name 
        = current_user.name
    .option
      .option__edit
        = = link_to new_group_path do
          = icon('far', 'edit')
      .option__cog
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')

  .group
    - current_user.groups.each do |group|
      .Group
        = link_to '#' do
          .Group__name
            = group.name
          .Group__message
            メッセージはまだありません
